const cursor = document.getElementById("cursor");
  
document.addEventListener("mousemove", e => {

cursor.setAttribute("style", "top: "+(e.pageY - 10)+"px;"+"left:"+(e.pageX - 10)+"px" );

});

// Get elemet by id input 

// .value 

[regex]

test('should first', () => { second }) // For searching 
match
// .contain(true) => validation

// .contain(false) => action : ex : location.reload();


// use Class1 , class3